name: 'Github actions'
description: 'Github actions for this repo'
author: 'PrajwalKoirala'
on: [push, pull_request]
steps:
- uses: actions/checkout@master
- uses: actions/setup-go@v1
  with:
    go-version: '1.14.1' # The Go version to download (if necessary) and use.
- run: go run main.go
