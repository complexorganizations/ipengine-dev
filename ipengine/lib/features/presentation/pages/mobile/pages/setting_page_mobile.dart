import 'package:flutter/material.dart';

class SettingPageMobile extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [Text("setting page mobile")],
      ),
    );
  }
}
